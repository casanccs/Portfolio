The files here use the Standard Python Library, but some use the "keyboard" module, which requires you to run "pip install keyboard".
"chess.py" is incomplete, but is easy to fix.
"Pacman2.py" I think has a small bug when colliding with an enemy.
